#include <iostream>
#include <fstream>
#include <string>

#include "ice.hpp"

int main()
{
    while(true)
    {
        // The instruction read

        // The instruction parse - Tokenisation

        // The instruction processing - Call the library
    }
    return 0;
}